# サイクル13 設計 セルフレビュー結果（UC-1-1）

レビュー対象：[cycle13_design.md](cycle13_design.md) FR-57・FR-58・FR-59
レビュー基準：[self_review_rule.md](self_review_rule.md)

---

## 1. 機能面

- [x] FR-57（画面定義書の是正）・FR-58（定性データの取得・保存・表示）・
  FR-59（命名是正）すべてに対応する設計がある
- [x] 各機能の入力（EDINET CSV／既存DB）・処理・出力（DB保存・API・画面表示）を
  具体的に記載している
- [x] エラー・例外ケース：定性データが存在しない企業（404）、選択年度にデータが
  ない場合（3項目null）を設計に含めた
- [x] ユーザーの全操作：年度セレクタでの切り替え、開閉式の展開/折りたたみを画面
  項目定義（SCR-003-14〜19）に含めた

## 2. 性能面

- [x] EDINET APIのレート制限：FR-58の通常運用（新規書類の取り込み時）は追加通信
  なし。FR-59のBATCH-005遡及分のみ約515リクエスト（ユーザー許可済み）
- [x] キャッシュ戦略：対象外（DBに保存済みのため都度EDINET通信しない設計）
- [x] XBRLパース：`parse_qualitative_facts`は`parse_quantitative_facts`と同じCSVを
  再利用し、追加のパース処理は軽量（対象要素3種類のみに絞り込み）

## 3. セキュリティ・運用面

- [x] APIキー：バックエンドのみで完結、フロントエンドに露出しない（既存方針と同じ）
- [x] 入力バリデーション：`period_end`はDateクエリパラメータとしてFastAPIが型検証
- [x] EDINETアクセスの原則：API-COM-006はDBのみ参照し、ユーザー操作時にEDINETへ
  アクセスしないことを明記した（サイクル9の原則を維持）

## 4. 拡張性

- [x] `company_qualitative_facts`は要素IDを行で持つ設計のため、将来対象テキスト
  ブロックが増えても列追加が不要
- [x] `CompanyQuantitativeFact`・`CompanyQualitativeFact`という対になる命名により、
  今後同種のデータを追加する際の命名基準ができた

---

## 5. 要件トレーサビリティチェック（FRの箇条書き単位で設計と突き合わせ）

- [x] FR-57「画面定義書を実装に合わせて更新する」→ 設計§2でB/S・P/L分離、
  財務分析指標セクションのグラフ化等を反映し、実際に`SCR-003_items.md`・
  `SCR-003_company_detail.md`を全面更新した
- [x] FR-58「対象テキストブロックは3種類」→ 設計§4`TARGET_QUALITATIVE_ELEMENT_IDS`
  で3値を明記
- [x] FR-58「新テーブルを新設する」→ 設計§4`CompanyQualitativeFact`モデル、
  マイグレーションまで具体化
- [x] FR-58「document_body_ingestionを拡張する」→ 設計§4で`ingest_document_body`
  拡張内容を記載、実装済み
- [x] FR-58「新規APIで最新/期間指定の定性情報を返す」→ 設計§3でAPI-COM-006の
  レスポンス仕様（`available_periods`含む）を具体化
- [x] FR-58「EDINETには一切アクセスしない」→ 設計§3に明記、実装・実機確認済み
- [x] FR-58「SCR-003に定性情報セクションを追加する」→ 設計§2で画面項目まで具体化。
  **訂正**：実装中のユーザー指示で、年度セレクタを財務グラフと独立させる仕様に
  変更し、設計書も追随して修正済み
- [x] FR-58「既存データへの遡及適用」→ 設計§4でBATCH-005の設計を記載
  （実行はこの後の実装フェーズで行う）
- [x] FR-59「対応表通りにリネームする」→ 設計§4に対応表を反映し、`database.py`・
  `xbrl_parser.py`・`quantitative_fact_ingestion.py`・`routers/`まで実装済み
- [x] FR-59「documents.facts_ingested_atもリネームする」→ 設計に追記し、
  マイグレーション・コードとも反映済み
- [x] FR-60「run_download_jobがdocumentsを更新するよう修正する」→ 設計§4に
  `upsert_document_from_row`の切り出し・呼び出し追加を記載し、実装・実機検証
  （会社コード1376）まで完了
- [x] FR-60「BATCH-005の対象をcompany_quantitative_facts基準に修正する」→
  設計§4のバッチ実装を修正し、実行対象485件を確認済み

**発見・修正した不一致**：
- 当初の設計は「定性情報は常に最新開示のみ表示」だったが、ユーザーから
  「過去の年度も遡って確認できるようにしたい」と指摘があり、独立した単一年度
  セレクタを持つ設計に訂正した（設計書上で「訂正」として明記済み）
- 当初の設計は「開示書類が1件もない企業はセクション自体を非表示」だったが、
  ユーザーから「パネルは常に表示し情報なしと明示してほしい」と指摘があり、
  常時表示に訂正した
- 実装中に、既存の`facts`テーブル自体の命名も曖昧だと判明し、FR-59として
  スコープに追加した（要件定義・設計とも追記済み）
- 実装中に、SCR-004（生データ確認画面）・API-COM-004がユースケースに紐づかない
  機能と判断され削除した（設計書・関連ドキュメントも追随して更新済み）
- 定性データの表示品質について、EDINETのCSV変換時点で表構造が失われる既知の
  制約を発見。対応は`cycleX_backlog.md`へ送り、今回のスコープには含めない
  （ユーザー判断）
- BATCH-005の対象件数を確認する過程で、個別ダウンロード機能（`routers/edinet.py`、
  SCR-001）が長年`documents`テーブルを一切更新していなかった不整合を発見した
  （FR-60として要件定義・設計に追加、根本原因を修正）。これは今回のFR-58/59の
  作業がなければ気づかれないまま蓄積し続けていた問題で、実データの確認
  （BATCH-005の対象件数が想定515件ではなく30件だった点）から発覚した

**DBスキーマ・バッチ追加の該当確認**：
- DBスキーマ：`TBL-003_company_quantitative_facts.md`（リネーム）・
  `TBL-005_company_qualitative_facts.md`（新規）・`er_diagram.md`・
  `table_list.md`をすべて更新済み
- バッチ：`BATCH-005_backfill_qualitative_facts.md`（新規）・`batch_list.md`を
  更新済み

---

## 判定：完了（実装フェーズへ進む）

設計フェーズ中に発見した3件の不一致（年度セレクタの仕様、データなし時の表示、
命名是正の追加スコープ）はすべてユーザー確認の上で設計書に反映済み。実装は
設計とほぼ並行して進めたため、実装内容と設計書の整合は既に取れている
（実装セルフレビューで最終確認する）。
