# サイクル7：全社展開時の所要時間見積もり

対象：[cycle7_requirements.md](../requirements/cycle7_requirements.md) FR-44
検証スクリプト：`backend/scripts/verify_batch_timing.py`（実データ、モックなし）

---

## 実測結果（サンプル20社、業種分散）

| code | 企業名 | 所要時間(秒) | リクエスト数 | ステータス |
|---|---|---|---|---|
| 1518 | 三井松島ホールディングス | 187.6 | 236 | DONE |
| 189A | 株式会社D&Mカンパニー | 346.1 | 428 | DONE |
| 3110 | 日東紡績 | 100.4 | 132 | DONE |
| 5101 | 横浜ゴム | 68.1 | 72 | DONE |
| 142A | 株式会社ジンジブ | 312.0 | 373 | DONE |
| 3708 | 特種東海製紙 | 76.8 | 100 | DONE |
| 1435 | 株式会社robot home | 95.5 | 116 | DONE |
| 377A | 株式会社エージェントIGホールディングス | 371.4 | 474 | DONE |
| 9193 | 東京汽船 | 51.5 | 66 | DONE |
| 247A | Aiロボティクス | 337.9 | 438 | DONE |
| 130A | 株式会社Veritas In Silico | 333.2 | 417 | DONE |
| 167A | リョーサン菱洋ホールディングス | 336.0 | 434 | DONE |
| 7699 | オムニ・プラス・システム・リミテッド | 491.9 | 610 | **ERROR** |
| 138A | 光フードサービス | 306.7 | 376 | DONE |
| 1401 | 株式会社エムビーエス | 25.4 | 34 | DONE |
| 135A | 株式会社VRAIN Solution | 283.4 | 372 | DONE |
| 1909 | 日本ドライケミカル | 127.1 | 150 | DONE |
| 1301 | 株式会社 極洋 | 0.0 | 0 | DONE（既存データ、EDINET未アクセス） |
| 9101 | 日本郵船 | 172.7 | 220 | DONE |
| 3315 | 日本コークス工業 | 42.6 | 52 | DONE |

**集計**：20社、合計4,066.5秒、合計5,100リクエスト。1社あたり平均**203.3秒／255.0リクエスト**
（極洋は既存データによりEDINET未アクセスのため、この1社を除く19社の平均は
**214.0秒／268.4リクエスト**）。中央値は約180秒。

---

## 全社展開（3,829社）時の試算

サイクル6時点で会計基準が判明済み＝財務データ取得済みなのは33社（サイクル5以前の14社＋
本サイクルの検証で成功した19社）。残り約3,796社が未取得。

| 基準 | 1社あたり | 3,829社換算 | 日数換算（24時間連続稼働） |
|---|---|---|---|
| 平均（20社、極洋含む） | 203.3秒 | 約778,600秒 | 約9.0日 |
| 平均（19社、極洋除く） | 214.0秒 | 約819,700秒 | 約9.5日 |
| 中央値 | 約180秒 | 約689,200秒 | 約8.0日 |

**現状の実装のまま全上場企業を一括取得すると、単一プロセスの逐次実行で約8〜10日間、
休みなく動かし続ける必要がある**という結果になった。

---

## データ量の見積もり（フェーズ6：SQLite/MySQL判断向け、追加データ取得なしで実測値から算出）

本サイクルの検証実行で得られた実データ（33社分の`facts`）をそのまま使い、追加の
EDINETアクセスなしで算出した。あくまで概算だが、フェーズ6（DB移行判断）・フェーズ3
（取り込みバッチの設計）双方に使える一次情報として残す。

| 項目 | 値 |
|---|---|
| 現在のDBファイルサイズ | 128.8 MB（企業マスタ3,829社＋facts 33社分） |
| facts取得済み企業数 | 33社 |
| facts総行数 | 310,483行 |
| 1社あたり平均facts行数 | 約9,409行 |
| factsの1行あたりの概算バイト数 | 約435バイト（DBファイルサイズ÷facts総行数） |
| **全社（3,829社）換算の推定facts行数** | **約3,602万行** |
| **全社換算の推定DBサイズ** | **約14.6 GB** |

### 評価（MySQL移行判断に向けて）

約3,600万行・約15GBという規模は、SQLiteが技術的に扱えない規模ではない
（SQLiteは仕様上テラバイト級・数十億行まで扱える）。したがって、**「サイズが理由で
SQLiteでは足りない」とは言えない**。MySQL等への移行を検討する動機があるとすれば、
サイズそのものよりも次のような運用面の理由になる：

- フェーズ4（定期更新バッチ）稼働時に、書き込み中の同時読み取り（アプリからの
  参照）が発生する。SQLiteはファイルロックベースで同時書き込みに弱く、
  複数プロセスからの並行アクセス（フェーズ3で検討中の並列化とも関係する）を
  行うならMySQL等の方が適する
- バックアップ・レプリケーション等の運用のしやすさ

**結論**：データ量そのものはSQLite継続の障害にならない。移行するかどうかは
「同時書き込み・並列処理をどこまでやるか」という運用設計（フェーズ3・4の設計内容）
次第であり、フェーズ6は「データ量」ではなく「並行アクセスパターン」を主な判断材料に
すべき、という見立てに更新する。

---

## 評価

この所要時間は実務上許容できない（サーバーを1週間以上専有し続けることになり、
途中でEDINET側の障害・アプリの再起動等が起きれば最初からやり直しのリスクも大きい）。
フェーズ3をこのまま「フェーズ1と同じ発想の一括バッチ」として実装するのは現実的でない。

さらに、失敗ケース（20社中1社、5%）は`search_report`が`window_days`の範囲を
最後まで探索してから諦めるため、成功ケースより顕著にコストが高い
（491.9秒/610リクエストで最大値）。全社規模では約190社（3,829社の5%）がこの
最悪パターンに該当する可能性があり、上記の試算より実際にはさらに時間がかかる
可能性が高い。

---

## フェーズ3への申し送り事項

### 発見した最大の改善余地：書類一覧APIの結果は企業非依存でキャッシュできる

`edinet_client.fetch_document_list(target_date)`は**指定日にEDINETへ提出された
全書類の一覧**を返す。company_codeやedinet_codeに依存しない。現在の実装は
`search_report`が企業ごとに独立して同じ日付の一覧を何度も取得しており、
決算期が近い企業同士（例：3月決算企業が多数）では、同じ日付の書類一覧を
何十回・何百回も重複して取得している可能性が高い。

**フェーズ3では、`fetch_document_list`の結果を日付単位でプロセス内キャッシュし、
全社のバッチ処理を通じて使い回す設計にすることを強く推奨する**。これだけで
リクエスト数を大幅に削減できる可能性がある（削減率はキャッシュ実装後に実測が必要）。

### その他の検討事項

- **並列化**：現在のレート制限（`MIN_REQUEST_INTERVAL_SECONDS`）はプロセス内のグローバル
  変数で管理されており、複数プロセスで並列実行した場合に実際のEDINET側のレート制限を
  超えないかは未検証。上記のキャッシュ導入後に、なお時間が足りない場合の次善策として検討する
- **`window_days`の縮小**：デフォルト25日が本当に必要か、実際の提出日のズレ幅を
  今回のログから分析し、縮小の余地がないか確認する（本サイクルでは個々の探索日数までは
  ログに残していないため、次回検証時に追加計測する）
- **失敗ケースの原因調査**：オムニ・プラス・システム・リミテッド（`code=7699`）は
  FR-21（サイクル3）で一度対応した既知の404企業。今回のサンプルにたまたま含まれており、
  「見つからない」ケースの実測データとして有用だった
- **段階的ロールアウト**：一度に全社を処理せず、優先度の高い企業群から段階的に
  処理するアプローチも検討に値する（例：業種・時価総額等での優先順位付け）

キャッシュ導入によるリクエスト削減効果を測ることが、フェーズ3着手前の
次の技術検証として最も価値が高い。

---

## サイクル8：キャッシュ導入後の再検証結果

対象：[cycle8_requirements.md](../requirements/cycle8_requirements.md) FR-45〜47
`fetch_document_list`に日付単位のプロセス内キャッシュを追加し、サイクル7と同条件
（未取得企業20社、業種分散）で再測定した。

| | サイクル7（キャッシュなし） | サイクル8（キャッシュあり） | 削減率 |
|---|---|---|---|
| 合計所要時間 | 4,066.5秒 | 2,118.9秒 | **約48%減** |
| 合計リクエスト数 | 5,100 | 2,625 | **約49%減** |
| 1社あたり平均 | 203.3秒／255.0リクエスト | 105.9秒／131.2リクエスト | 同上 |

サイクル8のサンプルでは、後半に処理した企業ほど所要時間が短くなる傾向が明確に出た
（例：17〜20社目は3.9秒〜66.7秒、リクエスト数も一桁〜数十件程度）。これは、先に
処理した企業が調べた日付が後続企業の探索でもヒットするようになるため。失敗ケース
（`code=7699`、既知の404企業）も491.9秒/610リクエストから113.2秒/148リクエストへと
半分以下に改善した。

**全社（3,829社のうち未取得の約3,796社）展開時の再試算**：1社あたり平均105.9秒として、
3,796社×105.9秒 ≈ 402,022秒 ≈ **約4.7日**。サイクル7時点の約8〜10日からは半減したが、
依然として実務上許容できる水準ではない。

### 結論：キャッシュ導入だけでは不十分。アーキテクチャ自体を見直す

キャッシュによる約2倍の改善は確認できたが、根本的な解決には至っていない。ユーザーとの
議論の中で、**そもそもEDINETの書類一覧API自体が「日付を指定して、その日の全提出書類を
取得する」という設計であり、「企業を指定して書類を取得する」機能はEDINET側に存在しない**
ことが明確になった（`docs/external/edinet/EDINET_API_仕様書.pdf` 3-1-1「リクエスト
パラメータ」で確認：`documents.json`のパラメータは`date`・`type`・`Subscription-Key`のみ）。

つまり、今の実装（企業ごとに提出日を推測し、日付を1日ずつずらしながら探す）は
EDINETの設計思想に逆行している。**EDINET本来の使い方は「毎日ポーリングし、その日に
何か提出されていたら随時取り込む」という方式**であり、これを踏まえると：

- 過去分の一括取得（フェーズ3）：今日から1日ずつ遡り、過去10年分（暦日で約3,650日）を
  日付ごとに1回ずつ取得する。1日1リクエスト×0.6秒間隔として、**合計約3,650リクエスト・
  約37分**で全期間・全企業分の書類一覧（索引）を作れる計算になる
  （現行方式の3,796社×105.9秒≈4.7日と比べて桁違いに速い）
- 継続的な更新（フェーズ4）：同じ仕組みで「今日」だけを毎日1回チェックすればよく、
  フェーズ3とフェーズ4が同じコードパスで実現できる

この場合、書類一覧APIのレスポンス（`docID`・`secCode`・`docTypeCode`・`periodStart`・
`periodEnd`・`submitDateTime`・`withdrawalStatus`・`disclosureStatus`等）をDBに保存し、
「書類そのもの」を表す新しいテーブルを設けることが前提になる（`facts`が値そのものを
持つのに対し、こちらは「どの書類が存在するか」の索引を持つ）。これにより再開可能性・
二重処理防止・訂正/取り下げ検知にも対応しやすくなる。

**次サイクルへの申し送り**：フェーズ3は「企業ごとの検索＋キャッシュ」ではなく、
「日付を毎日ポーリングして書類一覧を取り込む」方式で要件定義からやり直す。
新しいテーブル（書類メタデータ）の設計を含める。

**フェーズ6（DB移行判断）の最終結論**：上記「データ量の見積もり」節の評価は
サイクル10で実データ（サイクル9実装後の状態）をもとに再計算・確定した。
結論と根拠は[cycle10_db_migration_decision.md](cycle10_db_migration_decision.md)を参照。
